{
  "name": "@termes/orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "esbuild src/main.ts --bundle --platform=node --target=node22 --format=cjs --outfile=dist/main.cjs --external:pg-native",
    "start": "node dist/main.cjs"
  },
  "dependencies": {
    "@termes/shared": "workspace:*",
    "fastify": "^5.4.0",
    "ioredis": "^5.6.1",
    "pg": "^8.16.2"
  },
  "devDependencies": {
    "@types/pg": "^8.15.4",
    "typescript": "^5.8.3"
  }
}
